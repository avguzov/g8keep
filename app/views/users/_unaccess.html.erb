<%= form_for current_user.relationships.find_by_accessed_id(@user),
             :html => { :method => :delete },
			 :remote => true do |f| %>
  <div class="actions"><%= f.submit "Close Gate" %></div>
<% end %>
