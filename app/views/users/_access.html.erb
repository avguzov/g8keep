<%= form_for current_user.relationships.
			build(:accessed_id => @user.id) do |f| %>
  <div><%= f.hidden_field :accessed_id %></div>
  <div class="actions"><%= f.submit "Open Gate" %></div>
<% end %>
